import { ListadoTagsPipe } from './listado-tags.pipe';

describe('ListadoTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListadoTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
